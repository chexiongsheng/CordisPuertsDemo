# CordisPuertsDemo

## 克隆项目

克隆本项目（会一并拉取 `cordis` 子模块）：

```bash
git clone --recurse-submodules git@github.com:chexiongsheng/CordisPuertsDemo.git
```

如果已经克隆但未初始化子模块：

```bash
git submodule update --init --recursive
```

## 安装puerts

用codebuddy打开本仓库，提示词输入“安装puerts”即可。
